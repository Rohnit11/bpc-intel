# /segment <geography> <segment>

One-page brief for a single geography × segment (e.g. `KR skincare`).

Lists every figure held for that segment with basis, period, confidence, and
source. If empty, points to the gaps register for suggested sources.

Run: `python -m lib.reports.snapshot segment KR skincare`
